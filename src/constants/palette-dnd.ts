/**
 * MIME type used when dragging a palette entry onto the canvas to add a new
 * equipment instance. The dataTransfer payload is the EquipmentType string.
 */
export const PALETTE_ADD_MIME = 'application/x-sentire-palette-add';
